#include <stdio.h>

/**
 * main- entry point
 *
 * Explanation: The program performs the required tasks.
 *
 * Return: Always 0 (Success)
 */

int main(void) 
{
char str[] = "_putchar";
int s;
for (s = 0; s < 8; s++)
_putchar(str[s]);
_putchar('\n');

return (0);
}
