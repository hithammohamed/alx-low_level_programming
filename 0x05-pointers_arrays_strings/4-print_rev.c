#include "main.h"
#include <string.h>

/**
 * print_rev - You print the words backwards
 *
 * @s: pointer to the string to print
 *
 * Return: void
**/

void print_rev(char *s)
{
	int string = 0;

	while (s[string])
		string++;
	while (i--)
	{
	putchar(s[string])
	}
	putchar('\n');

}
